# Thư viện tính toán số học (Bắt buộc)
numpy>=1.20.0

# Thư viện trực quan hóa (Bắt buộc)
matplotlib>=3.3.0
seaborn>=0.11.0

# Môi trường chạy Notebook (Nếu cần cài đặt lại)
jupyter>=1.0.0
notebook>=6.0.0